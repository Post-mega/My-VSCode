/**
 *
 *
 * @param {*} arr
 * @returns
 */
function quickSort(arr) {
    if(arr.length<=1){
        return arr;
    }
    var less = new Array();
    var greater = new Array();
    var pivotIndex = Math
}